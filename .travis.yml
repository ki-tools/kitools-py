sudo: false
language: python
python:
  - "3.5"
  - "3.6"
before_install:
  - export LOG_LEVEL=DEBUG
install:
  - pip install tox-travis coveralls
env:
  - PUBLISH_COVERAGE_COMMAND=coveralls
script:
  - tox
